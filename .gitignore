# Local DuckDB (override path with JD_STAGING_DATA_DIR)
data/*.duckdb
data/*.db

# Optional EPUB library for batch ingest (override with JD_BOOKS_DIR)
books/*
!books/.gitkeep

# Generated next to the DB when embedding
**/jds_books_duckdb_manifest.json

# Local Chroma / embedding data � do not commit
archive/chromadb/chroma_persist/*

# Chroma data folder � do not commit
data/chroma_db/*

# cursor workspace files
*.code-workspace

# misc files
.idea/
.vscode/

# set env vars on local machine and run pipeline locally
local_run/*
# How to set JD_STAGING_DATA_DIR / JD_BOOKS_DIR: see README.md (Setup). Implemented in script/local_paths.py.

# python cache files
__pycache__/
*.py[cod]
